package main

import "fmt"

func main() {
	// for range 是Go特有的一种迭代结构，它可以迭代任何一个集合
	// 如数组和map
	// 一般形式为: for ix, val := range coll {}
	// val 为集合中对应索引值的拷贝

	// 一个字符串是Unicode 编码的字符(或称之为rune)集合，因此可以用它迭代字符串
	// for pos,char := range str {}
}